{
  "name": "vwo-playwright-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright demo project for the pw-locator-fixer skill",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0"
  }
}
